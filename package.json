{
  "name": "gitark",
  "scripts": {
    "dev": "web-ext run",
    "build": "web-ext build"
  },
  "webExt": {
    "build": {
      "overwriteDest": true
    },
    "run": {
      "startUrl": ["github.com"]
    },
    "ignoreFiles": [
      "package-lock.json",
      "package.json"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trs/gitark.git"
  },
  "license": "GNU",
  "devDependencies": {
    "web-ext": "^3.0.0"
  }
}
